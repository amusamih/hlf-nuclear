{
  "name": "@prototype/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:chaincode": "tsc -p tsconfig.chaincode.json",
    "lint": "node -e \"console.log('shared: no linter configured yet')\"",
    "test": "node -e \"console.log('shared: no tests configured yet')\""
  }
}
